{% extends 'base.html.twig' %}
{% block body %}
    {% include 'navbar.html.twig' %}

    <div class="container">

        {% if mod_guilds|default %}
            <span class="display-4 mb-3">Moderator Guilds ({{ mod_guilds|length }})</span>
            <div class="d-flex flex-column flex-wrap justify-content-between mt-3">
                {% for guild in mod_guilds %}
                    <a href="/modcases/{{ guild.obj.id }}" class="mb-3">
                        <div class="card">
                            <div class="card-body d-flex flex-row">
                                <div class="flex-grow-1">
                                    <!-- Title -->
                                    <h4 class="card-title"> {{ guild.obj.name }} </h4>

                                    <!-- Text -->
                                    <div class="card-text">
                                        <p>#{{ guild.obj.id }}</p>
                                        <p class="font-weight-bold">Cases: {{ guild.stats.modcase_count }}</p>
                                    </div>
                                </div>
                                <div class="align-self-end">
                                    <img class="card-img"
                                         src="https://cdn.discordapp.com/icons/{{ guild.obj.id }}/{{ guild.obj.icon }}.png"
                                         alt="Guild icon"/>
                                </div>
                            </div>
                            <div class="card-text">
                            </div>
                        </div>
                    </a>
                {% endfor %}
            </div>
        {% endif %}

        {% if member_guilds|default %}
            {% if mod_guilds|default %}
                <hr/>
            {% endif %}
            <span class="display-4 mb-3">Guilds ({{ member_guilds|length }})</span>
            <div class="d-flex flex-column flex-wrap justify-content-between mt-3">
                {% for guild in member_guilds %}
                    <a href="/modcases/{{ guild.obj.id }}" class="mb-3">
                        <div class="card">
                            <div class="card-body d-flex flex-row">
                                <div class="flex-grow-1">
                                    <!-- Title -->
                                    <h4 class="card-title"> {{ guild.obj.name }} </h4>

                                    <!-- Text -->
                                    <div class="card-text">
                                        <p>#{{ guild.obj.id }}</p>
                                        <p class="font-weight-bold">Cases: {{ guild.stats.modcase_count }}</p>
                                    </div>
                                </div>
                                <div class="align-self-end">
                                    <img class="card-img"
                                         src="https://cdn.discordapp.com/icons/{{ guild.obj.id }}/{{ guild.obj.icon }}.png"
                                         alt="Guild icon"/>
                                </div>
                            </div>
                            <div class="card-text">
                            </div>
                        </div>
                    </a>
                {% endfor %}
            </div>
        {% endif %}

        {% if banned_guilds|default %}
            {% if mod_guilds|default or member_guilds|default %}
                <hr/>
            {% endif %}
            <span class="display-4 mb-3">Banned from Guilds ({{ banned_guilds|length }})</span>
            <div class="d-flex flex-column flex-wrap justify-content-between mt-3">
                {% for guild in banned_guilds %}
                    <a href="/modcases/{{ guild.obj.id }}" class="mb-3">
                        <div class="card">
                            <div class="card-body d-flex flex-row">
                                <div class="flex-grow-1">
                                    <!-- Title -->
                                    <h4 class="card-title"> {{ guild.obj.name }} </h4>

                                    <!-- Text -->
                                    <div class="card-text">
                                        <p>#{{ guild.obj.id }}</p>
                                        <p class="font-weight-bold">Cases: {{ guild.stats.modcase_count }}</p>
                                    </div>
                                </div>
                                <div class="align-self-end">
                                    <img class="card-img"
                                         src="https://cdn.discordapp.com/icons/{{ guild.obj.id }}/{{ guild.obj.icon }}.png"
                                         alt="Guild icon"/>
                                </div>
                            </div>
                            <div class="card-text">
                            </div>
                        </div>
                    </a>
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock body %}
