{% extends 'base.html.twig' %}
{% block body %}
    {% include 'navbar.html.twig' %}

    <style>
        .hiddencolumn {
            display: none;
        }
    </style>

    <div class="container">
        <div class="d-flex justify-content-between align-items-start">
            <h3><span class="badge badge-warning">New WarnCase</span><strong> for User {{ userid }}</strong></h3>
        </div><br>


        {{ form_start(createWarnForm) }}

        <div class="md-form">
            {{ form_label(createWarnForm.userid) }}
            {{ form_widget(createWarnForm.userid, {'attr': {'placeholder': userid }}) }}
        </div>
        <div class="md-form">
            {{ form_row(createWarnForm.username) }}
        </div>
        <div class="md-form">
            {{ form_row(createWarnForm.nickname) }}
        </div>
        <div class="md-form">
            {{ form_row(createWarnForm.title) }}
        </div>
        <div class="md-form">
            {{ form_row(createWarnForm.occuredAt) }}
            <small class="form-text text-muted">
                Please use the following time format for documenting date and time of the case: dd.MM.YYYY HH:mm
            </small>
        </div>
        <div class="md-form">
            {{ form_row(createWarnForm.severity) }}
        </div>
        <div class="md-form">
            {{ form_row(createWarnForm.label) }}
        </div>
        <div class="md-form">
            {{ form_row(createWarnForm.description) }}
            <small class="form-text text-muted">
                Please describe the case as detailed as possible. You can also add links for proofs in this field.
            </small>
        </div>

        {{ form_end(createWarnForm) }}

    </div>


{% endblock body %}

{% block scriptblock %}

    <script>
        $(document).ready(function () {

            $('#dtBasicExample').DataTable();
        });
    </script>
{% endblock %}
