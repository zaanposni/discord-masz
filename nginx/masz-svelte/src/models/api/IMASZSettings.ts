export interface IMASZSettings {
    id: number;
    embedTitle: string;
    embedContent: string;
    embedShowIcon: boolean;
    defaultLanguage: number;
    auditLogWebhookURL: string;
    publicFileMode: boolean;
}
