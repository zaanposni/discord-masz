import { FloorPipePipe } from './floor-pipe.pipe';

describe('FloorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FloorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
